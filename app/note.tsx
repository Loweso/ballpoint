import { View } from "react-native";
import { Link } from "expo-router";
import React from "react";

const Note = () => {
  return (
    <View>
      <Link href="/">back</Link>
    </View>
  );
};

export default Note;
